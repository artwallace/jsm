import { actor2dbase } from '../../../engine/actor2dbase.js';

export class paratrooper extends actor2dbase {
    constructor(game) {
        super(game, 0, 0, 0);
    }

    update(delta) {
        super.update(delta);
    }

    draw(interp) {
        super.draw(interp);
    }
}